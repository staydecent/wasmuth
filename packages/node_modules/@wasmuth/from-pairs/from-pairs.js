import check from "check-arg-types";

export default function fromPairs(ls) {
  check(arguments, ["array"]);
  const out = {};
  for (let x = 0; x < ls.length; x++) {
    out[ls[x][0]] = ls[x][1];
  }
  return out;
}
